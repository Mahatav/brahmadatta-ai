"""Mission worker stages.

D-016: this package exists because code landed in it, not as a pre-built scaffold. Only
`workers/baseline/` has code today — the C/C++ baseline stage for #17. Other worker stages
(fuzzing, patching, correlation) are other roles' issues and are not present here.
"""

from __future__ import annotations
