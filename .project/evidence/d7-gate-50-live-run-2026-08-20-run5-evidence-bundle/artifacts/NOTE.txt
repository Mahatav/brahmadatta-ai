Raw stage artifacts (build logs, crash inputs, patch diffs as separate files) are not copied into this directory in this build — no function in this codebase yet resolves an ArtifactRef back to its stored bytes generically. The diffs and sanitized text this bundle can vouch for are already inline in report.json/report.md. Disclosed here rather than silently ignoring `include_artifacts=true`.