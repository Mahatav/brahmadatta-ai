# TLS material for the finale Postgres image is generated, never committed.
# The repository .gitignore already refuses *.pem and *.key everywhere; this file makes the
# rule local and obvious, and keeps the directory itself in git so the build context has
# something to COPY from before the generation script has run.
*
!.gitignore
